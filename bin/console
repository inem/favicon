#!/usr/bin/env ruby
# frozen_string_literal: true

require "bundler/setup"
require "favicon_gem"

# Можно использовать pry для лучшей отладки
# require "pry"
# Pry.start

require "irb"
IRB.start(__FILE__)